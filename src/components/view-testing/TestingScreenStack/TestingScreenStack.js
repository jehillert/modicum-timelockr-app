import { TestingScreen } from '@view-testing';
import { createStackScreen } from '@common';

const TestingScreenStack = createStackScreen(TestingScreen, 'Noke Android API');

export default TestingScreenStack;
